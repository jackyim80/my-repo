apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: "2025-03-08T09:34:05Z"
  generation: 1
  labels:
    app: web-pod
  name: web-pod
  namespace: default
  resourceVersion: "29806"
  uid: 0349a0ab-6b9f-4ca2-9eeb-8620e35bfb96
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: web-pod
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: web-pod
    spec:
      containers:
      - image: nginx
        imagePullPolicy: Always
        name: nginx
        ports:
        - containerPort: 80
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:                   # ✅ fieldRef 를 꼭 넣어야 함
              fieldPath: metadata.name
        - name: POD_IP
          valueFrom:
            fieldRef:                   # ✅ fieldRef 를 꼭 넣어야 함
              fieldPath: status.podIP
        - name: NODE_NAME        # ✅ Pod이 실행 중인 Node 이름
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
